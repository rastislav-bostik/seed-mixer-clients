# Shared assets

Drop here:
- App icons (1024×1024 master, exported per-platform from `app-icon.png`)
- Logo SVG
- Fixture images for offline development / screenshots
- Splash assets

Per-client build steps copy or reference these at build time.
